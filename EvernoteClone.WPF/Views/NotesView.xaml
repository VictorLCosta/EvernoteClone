<Window x:Class="EvernoteClone.WPF.Views.NotesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EvernoteClone.WPF.Views"
        xmlns:uc="clr-namespace:EvernoteClone.WPF.UserControls"
        mc:Ignorable="d"
        Title="NotesView"
        Icon="/Views/Notes.ico"
        Height="450" 
        Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="New notebook" Command="{Binding NewNotebookCommand}" />
                <MenuItem Header="New note" 
                          Command="{Binding NewNoteCommand}"
                          CommandParameter="{Binding SelectedNotebook}" />
                <Separator />
                <MenuItem Header="Exit" Click="MenuItem_Click" />
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" />
            </StatusBarItem>
        </StatusBar>
        <ListView x:Name="NotebookList"
                  DockPanel.Dock="Left"
                  Width="140"
                  Background="DarkBlue"
                  ItemsSource="{Binding Notebooks}"
                  SelectedItem="{Binding SelectedNotebook}">
            <ListView.Resources>
                <ContextMenu x:Key="NotebookContextMenu">
                    <MenuItem Header="Delete notebook"
              Command="{Binding Source={x:Reference NotebookList}, 
                                Path=DataContext.DeleteNotebookCommand}"
              CommandParameter="{Binding PlacementTarget.DataContext, 
                                         RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                </ContextMenu>
            </ListView.Resources>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="ContextMenu" Value="{StaticResource NotebookContextMenu}" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <uc:DisplayNotebook Notebook="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView x:Name="NotesList"
                  DockPanel.Dock="Left"
                  Width="140"
                  ItemsSource="{Binding Notes}"
                  SelectedItem="{Binding SelectedNote}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <uc:DisplayNote Note="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="Speech" 
                        Click="SpeechButton_Click" />
                <ToggleButton x:Name="BoldButton"
                        Content="B" 
                        FontWeight="Bold"
                        Click="BoldButton_Click" />
                <ComboBox x:Name="CbFontFamily"
                          Width="100"
                          SelectionChanged="CbFontFamily_SelectionChanged" />
                <ComboBox x:Name="CbFontSize"
                          IsEditable="True"
                          Width="50"
                          SelectionChanged="CbFontSize_SelectionChanged" />
                <Button x:Name="SaveButton"
                        Content="Save"
                        Click="SaveButton_Click" />
            </ToolBar>
        </ToolBarTray>
        <Grid>
            <RichTextBox x:Name="ContentRichTextBox"
                         
                         TextChanged="ContentRichTextBox_TextChanged"
                         SelectionChanged="ContentRichTextBox_SelectionChanged" />
        </Grid>
    </DockPanel>
</Window>
